
function x(callback, array) { }

function agregarElementoAlInicioArray(array, element) {
    array.unshift(element);
}


function agregarElementoAlFinalArray(array, element) {
    array.push(element);
}

const eliminar = (array) => { array.pop() }